# yaml-language-server: $schema=https://volland.github.io/dr-json-ld/schemas/project/1/project.schema.json
project: "1"
name: credential
note: A credential vocabulary with shapes, for trying the shapes layer and L3.
models:
  credential: credential.jsonld.yaml
