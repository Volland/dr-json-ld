/**
 * The scaffold a new model starts from, shared with `ldm init`.
 *
 * It is core's, re-exported under the name this package has always used. The
 * bytes the extension writes and the bytes the CLI writes are the same bytes
 * because they are the same constant, not because a test says so.
 *
 * @lat: [[metamodel#Metamodel#Stable Element IDs]]
 */
export { MODEL_SCAFFOLD as SCAFFOLD } from '@json-ld-modeler/core'
