# raises: L1.shape-field-unresolved
# No term defines `nickname` and the model declares no @vocab.
jsonld: "1"
namespace:
  prefix: ex
  base: https://example.org/ns#
mode: "1.1"
prefixes:
  xsd: http://www.w3.org/2001/XMLSchema#
terms:
  Person: { id: per001, "@id": ex:Person }
shapes:
  Person:
    id: shp001
    targetClass: Person
    fields:
      nickname: { max: 1 }
