# reject: containerCombination
jsonld: "1"

namespace:
  prefix: ex
  base: https://example.org/ns#

terms:
  # @graph pairs with @id or @index. @type is neither.
  bad: { "@id": "ex:a", "@container": ["@graph", "@type"] }
