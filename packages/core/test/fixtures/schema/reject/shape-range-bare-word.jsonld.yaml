# reject: field
# A bare word is neither a range keyword nor a datatype IRI.
jsonld: "1"

namespace:
  prefix: ex
  base: https://example.org/ns#

terms:
  Person: { "@id": ex:Person }
  name: { "@id": ex:name }

shapes:
  Person:
    targetClass: Person
    fields:
      name: { range: string }
